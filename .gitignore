# ไม่ track ไฟล์ที่สร้างโดย installer หรือ runtime
install.lock
uploads/*
!uploads/.htaccess
!uploads/avatars/
uploads/avatars/*
!uploads/avatars/.htaccess

# ไม่ track config ที่มีรหัสผ่าน
config/db.php

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
